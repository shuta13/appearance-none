import repo from '~/repositories/article';

export function getArticleForRNX() {
  return {
    // async invoke(params: { id: string }) {
    //   const result = await repo.getArticleForRNX(params);
    //   return result;
    // },
  };
}
