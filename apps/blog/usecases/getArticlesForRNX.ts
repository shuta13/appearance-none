import repo from '~/repositories/article';

export function getArticlesForRNX() {
  return {
    // async invoke({ tag }: { tag: string }) {
    //   const result = await repo.getArticlesForRNX({
    //     tag: tag,
    //   });
    //   return result;
    // },
  };
}
// export type GetArticlesResults = Awaited<
//   ReturnType<ReturnType<typeof getArticlesForRNX>['invoke']>
// >;
